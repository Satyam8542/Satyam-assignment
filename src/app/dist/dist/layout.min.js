'use client';

import './globals.css';
import { Inter } from 'next/font/google';
import Image from 'next/image';
import Link from 'next/link';
import React from 'react';

const inter = Inter({ subsets: ['latin'] });

export const metadata = {
  title: 'Practo Clone',
  description: 'Search doctors, clinics, hospitals',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <header className="w-full bg-white text-black shadow-sm sticky top-0 z-50">
          <div className="max-w-7xl mx-auto px-4 py-3 flex items-center justify-between">
            {/* Left - Logo */}
            <div className="flex items-center gap-2">
              <Image
                src="/Practologo.png"
                alt="Logo"
                width={100}
                height={24}
              />
            </div>

            {/* Center - Navigation */}
            <nav className="hidden md:flex gap-6 text-sm font-medium items-left justify-left">
              <Link href="/" className="hover:text-blue-600">
                Find Doctors
              </Link>
              <a href="#" className="hover:text-blue-600">
                Video Consult
              </a>
              <a href="#" className="hover:text-blue-600">
                Surgeries
              </a>
            </nav>

            {/* Right - Menu + Auth buttons */}
            <div className="dropdown">
              <button className="dropbtn">
                Menu <i className="arrow down" />
              </button>
              <div className="dropdown-content">
                <a href="#">Home</a>
                <a href="#">About</a>
                <a href="#">Contact</a>
              </div>
            </div>

            <div>
              <button className="text-sm hover:text-blue-600">Login</button>
              <button className="text-sm hover:bg-blue-700">Sign up</button>
            </div>
          </div>
        </header>

        <main>{children}</main>
      </body>
    </html>
  );
}
